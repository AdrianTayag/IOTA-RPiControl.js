const Gpio = require('onoff').Gpio
const Z1 = new Gpio(19, 'in', 'both', {debounceTimeout: 10})
const PCC = new Gpio(19, 'out')

Z1.watch((err, value) => {
  if (err) {
    throw err
  }
  PCC.writeSync(value)
})
