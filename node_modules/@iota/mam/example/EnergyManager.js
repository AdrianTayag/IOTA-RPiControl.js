//launches protection coordinator and fetches data from SourceController to send via MAM
// Inputs: Island/Connect to grid, Disable specific MS, Grid Status prompt
// Outputs: Grid status (per minute, unless prompted), Cumulative production of each MS
// Internal Inputs: Grid status, Per minute MS Production
// Internal Outputs: Disable MS,  Islanding Command, request data

const PC = require('Protecc.js')
const MS = require('SourceController.js')
